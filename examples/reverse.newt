program Reverse {
  Integer n := console.readInteger();
  Integer nums[n];

  for (Integer i := 0; i < n; i := i+1) {
    nums[n-i-1] := console.readInteger();
  }

  for (Integer i := 0; i < n; i := i+1) {
    console.write(nums[i]);
  }
}
